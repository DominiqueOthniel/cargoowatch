# Configuration MongoDB (à venir)
# MONGODB_URI=mongodb://localhost:27017/cargowatch
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cargowatch

# Configuration du serveur
PORT=3000
NODE_ENV=development

# Secret pour les sessions (généré automatiquement)
# Générez un secret fort avec: openssl rand -base64 32
SESSION_SECRET=your-super-secret-key-change-in-production

# Optionnel: Redis pour les sessions et Socket.io (pour production/Vercel)
# REDIS_URL=redis://localhost:6379
