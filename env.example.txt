# Configuration Supabase
# Obtenez ces valeurs depuis https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://xkowxkwinqmeivelazwp.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
# Alternative: vous pouvez aussi utiliser SUPABASE_KEY au lieu de SUPABASE_ANON_KEY
# SUPABASE_KEY=your-anon-key-here

# Activez Supabase (true) ou utilisez les fichiers JSON (false)
USE_SUPABASE=true

# Configuration du serveur
PORT=3000
NODE_ENV=development

# Secret pour les sessions (changez-le en production !)
# Générez un secret fort avec: openssl rand -base64 32
SESSION_SECRET=your-super-secret-key-change-in-production

# Optionnel: Redis pour les sessions et Socket.io (pour production/Vercel)
# REDIS_URL=redis://localhost:6379

